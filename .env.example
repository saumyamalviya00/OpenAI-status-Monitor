# Copy this file to .env and edit values as needed
# Optional HMAC secret for webhook verification (leave blank to disable signature checks)
SIGNING_SECRET=

# Optional Redis URL if you want dedup persisted across restarts
# For local development, you can leave this blank to use in-memory storage
REDIS_URL=

# Set to 1 to enable fallback poller that fetches summary.json from the status page
ENABLE_POLLER=1

# Status page summary URL to poll (when poller is enabled)
STATUS_SUMMARY_URL=https://status.openai.com/api/v2/summary.json

# How often to poll the status page (in seconds)
POLL_INTERVAL_SECONDS=30

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO